---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - apps/dashboard/app/page.tsx
  - apps/dashboard/app/layout.tsx
  - apps/dashboard/app/clientes/page.tsx
  - apps/dashboard/app/clientes/[id]/page.tsx
  - apps/dashboard/app/alertas/page.tsx
  - apps/dashboard/app/tareas/page.tsx
  - apps/dashboard/app/herramientas/page.tsx
  - apps/dashboard/app/herramientas/n8n/page.tsx
  - apps/dashboard/components/TareasCliente.tsx
autonomous: true
requirements:
  - DSYS-01
  - DSYS-04

must_haves:
  truths:
    - "Ningún archivo de página contiene referencias a los tokens oscuros viejos (--s1, --s2, --s3, --txt, --txt2, --txt3, --border2)"
    - "Ningún archivo contiene las clases CSS eliminadas (hover-row, hover-row2, hover-border, nav-item)"
    - "La página de clientes (/clientes) tiene chips de filtro funcionales que filtran filas por estado"
    - "La tabla de clientes tiene encabezados clickeables para ordenar por columna"
    - "El build compila sin errores TypeScript tras la migración completa"
  artifacts:
    - path: "apps/dashboard/app/clientes/page.tsx"
      provides: "Vista de clientes con filtros y tabla ordenable"
      contains: "useState"
    - path: "apps/dashboard/app/page.tsx"
      provides: "Dashboard migrado a tokens nuevos"
  key_links:
    - from: "apps/dashboard/app/clientes/page.tsx"
      to: "apps/dashboard/app/globals.css"
      via: "var(--surface-1), var(--text-1) — tokens nuevos"
      pattern: "var\\(--surface-1\\)"
    - from: "apps/dashboard/components/TareasCliente.tsx"
      to: "apps/dashboard/app/globals.css"
      via: "var(--surface-2), var(--border-2) — tokens migrados"
      pattern: "var\\(--surface-2\\)"
---

<objective>
Migrar los 9 archivos existentes (8 páginas + TareasCliente.tsx) de tokens CSS oscuros a los nuevos tokens Notion-style. Agregar filtros de tabla (chips de filtro + ordenación por columna) a la vista de clientes para cumplir DSYS-04.

Purpose: Completar la migración visual del design system en todo el codebase existente. Sin este plan, las páginas existentes seguirán usando tokens que ya no existen en globals.css (los oscuros --s1, --txt, etc.) y mostrarán valores undefined. DSYS-04 requiere vistas tipo tabla/database con filtros — la página de clientes es el candidato natural.

Output: 9 archivos migrados + app/clientes/page.tsx con filtros interactivos funcionales.
</objective>

<execution_context>
@/Users/juancruzmagallanes/Documents/Claude/get-shit-done/workflows/execute-plan.md
@/Users/juancruzmagallanes/Documents/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Tabla de mapping de tokens — copiar exactamente, no interpretar -->

| Token viejo (buscar) | Token nuevo (reemplazar) | Notas |
|---------------------|--------------------------|-------|
| `var(--txt)` | `var(--text-1)` | |
| `var(--txt2)` | `var(--text-2)` | |
| `var(--txt3)` | `var(--text-3)` | |
| `var(--s1)` | `var(--surface-1)` | |
| `var(--s2)` | `var(--surface-2)` | |
| `var(--s3)` | `var(--surface-2)` | sin surface-3 nuevo — mapear a surface-2 |
| `var(--border2)` | `var(--border-2)` | guión, no número |

Clases CSS a eliminar (reemplazar con Tailwind hover utilities):
| Clase vieja (buscar) | Reemplazo |
|---------------------|-----------|
| `hover-row` | `hover:bg-[var(--surface-1)]` |
| `hover-row2` | `hover:bg-[var(--surface-1)]` |
| `hover-border` | eliminar o reemplazar por inline hover |
| `nav-item` | eliminado — Sidebar nuevo maneja su propio estado |

<!-- Contrato de tabla/filtros del UI-SPEC para DSYS-04 -->

Filter chips row (encima de la tabla):
- display: flex, gap: 8px, flex-wrap wrap, padding: 12px 0
- chip default: bg var(--surface-1), border var(--border), text var(--text-2), padding 4px 8px, border-radius 20px, font-size 12px
- chip active: bg var(--accent-bg), border rgba(35,131,226,0.3), text var(--accent)
- chip hover: bg var(--surface-2)

Table header:
- bg var(--surface-1), height 36px, padding 0 16px
- th: 12px, weight 400, text-3, uppercase, letter-spacing 0.05em
- sort indicator: ArrowUp/ArrowDown de lucide-react, 12px, margin-left 4px

Table body rows:
- height 44px, padding 0 16px, hover: bg var(--surface-1)
- border-bottom: 1px solid var(--border)

Chips de filtro para la vista de clientes — por estado:
- "Todos" (sin filtro), "Estable", "Atención", "En Riesgo", "Crítico"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrar tokens CSS en 9 archivos existentes</name>

  <read_first>
    - apps/dashboard/app/page.tsx (leer completo — buscar todos los tokens viejos)
    - apps/dashboard/app/clientes/page.tsx (leer completo)
    - apps/dashboard/app/clientes/[id]/page.tsx (leer completo)
    - apps/dashboard/app/alertas/page.tsx (leer completo)
    - apps/dashboard/app/tareas/page.tsx (leer completo)
    - apps/dashboard/app/herramientas/page.tsx (leer completo)
    - apps/dashboard/app/herramientas/n8n/page.tsx (leer completo)
    - apps/dashboard/components/TareasCliente.tsx (leer completo)
    - .planning/phases/01-foundation/01-PATTERNS.md líneas 724–790 (tabla de sustitución y ejemplos concretos)
  </read_first>

  <files>
    apps/dashboard/app/page.tsx,
    apps/dashboard/app/clientes/page.tsx,
    apps/dashboard/app/clientes/[id]/page.tsx,
    apps/dashboard/app/alertas/page.tsx,
    apps/dashboard/app/tareas/page.tsx,
    apps/dashboard/app/herramientas/page.tsx,
    apps/dashboard/app/herramientas/n8n/page.tsx,
    apps/dashboard/components/TareasCliente.tsx
  </files>

  <action>
En cada uno de los 9 archivos, aplicar el mapping de tokens de la tabla de la sección `<interfaces>`. Las reglas son:

1. `var(--txt)` → `var(--text-1)` en TODOS los archivos
2. `var(--txt2)` → `var(--text-2)` en TODOS los archivos
3. `var(--txt3)` → `var(--text-3)` en TODOS los archivos
4. `var(--s1)` → `var(--surface-1)` en TODOS los archivos
5. `var(--s2)` → `var(--surface-2)` en TODOS los archivos
6. `var(--s3)` → `var(--surface-2)` (no existe surface-3 — mapear al más cercano)
7. `var(--border2)` → `var(--border-2)` en TODOS los archivos (atención: guión, no número)

**Para clases CSS helper:**
- Eliminar `hover-row` de los className. Reemplazar por `hover:bg-[var(--surface-1)]` en el mismo className.
- Eliminar `hover-row2` de los className. Reemplazar por `hover:bg-[var(--surface-1)]`.
- Eliminar `hover-border` de los className (usualmente en clientes/[id]/page.tsx).
- Eliminar `nav-item` y `nav-item active` de los className (probablemente no están en las páginas, solo en Sidebar.tsx viejo que ya fue reescrito).

**Ejemplos de referencia del PATTERNS.md:**

app/page.tsx línea 24 (antes → después):
```typescript
// ANTES: style={{ color: 'var(--txt)' }}
// DESPUÉS: style={{ color: 'var(--text-1)' }}
```

app/page.tsx línea 75 (antes → después):
```typescript
// ANTES: style={{ background: 'var(--s1)', border: `1px solid ${kpi.accent ? '...' : 'var(--border)'}` }}
// DESPUÉS: style={{ background: 'var(--surface-1)', border: `1px solid ${kpi.accent ? '...' : 'var(--border)'}` }}
```

app/page.tsx línea 115 (antes → después):
```typescript
// ANTES: className="hover-row flex items-center justify-between px-5 py-4 transition-colors"
// DESPUÉS: className="flex items-center justify-between px-5 py-4 transition-colors hover:bg-[var(--surface-1)]"
```

components/TareasCliente.tsx línea 98 (antes → después):
```typescript
// ANTES: style={{ background: 'var(--s3)', color: 'var(--txt2)', border: '1px solid var(--border2)' }}
// DESPUÉS: style={{ background: 'var(--surface-2)', color: 'var(--text-2)', border: '1px solid var(--border-2)' }}
```

**IMPORTANTE — No romper la lógica existente:**
- En app/clientes/[id]/page.tsx, la línea `const { id } = await params` y el pattern `export default async function ClientePage({ params }: { params: Promise<{ id: string }> })` NO se modifican.
- En app/tareas/page.tsx, toda la lógica de useState/localStorage NO se modifica.
- En components/TareasCliente.tsx, toda la lógica de tareas NO se modifica — solo los tokens CSS inline.
- Las clases Tailwind semánticas de color (`text-emerald-400`, `text-red-400`, `text-amber-400`, `bg-emerald-400/10`) NO se cambian — son corrects per CLAUDE.md.

**Archivos a revisar además** (por si contienen tokens viejos):
- apps/dashboard/app/herramientas/ghl/page.tsx (si existe)
- apps/dashboard/app/herramientas/openai/page.tsx (si existe)
- Estos también deben ser migrados si existen.

**Verificación post-migración:**
```bash
grep -rn "var(--s1)\|var(--s2)\|var(--s3)\|var(--txt)\|var(--txt2)\|var(--txt3)\|var(--border2)" \
  apps/dashboard/app/ apps/dashboard/components/TareasCliente.tsx
```
El comando debe retornar 0 líneas. Si retorna algo, es un token sin migrar.

```bash
grep -rn "hover-row\|hover-row2\|hover-border\b" \
  apps/dashboard/app/ apps/dashboard/components/TareasCliente.tsx
```
El comando debe retornar 0 líneas.
  </action>

  <verify>
    <automated>cd /Users/juancruzmagallanes/Documents/Claude/Programador:Desarrollador/apps/dashboard && npm run build 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - grep -r "var(--s1)\|var(--s2)\|var(--s3)\|var(--txt)\b\|var(--txt2)\|var(--txt3)\|var(--border2)" apps/dashboard/app/ apps/dashboard/components/TareasCliente.tsx retorna 0 líneas
    - grep -r "hover-row\|hover-row2\|hover-border\b" apps/dashboard/app/ apps/dashboard/components/TareasCliente.tsx retorna 0 líneas
    - apps/dashboard/app/page.tsx contiene `var(--text-1)` o `var(--surface-1)` (confirma migración)
    - apps/dashboard/components/TareasCliente.tsx contiene `var(--surface-2)` (confirma migración de --s3)
    - `npm run build` sale con código 0
  </acceptance_criteria>

  <done>Los 9 archivos migrados de tokens oscuros a tokens Notion-style. El grep de verificación retorna 0 resultados para todos los tokens viejos.</done>
</task>

<task type="auto">
  <name>Task 2: Agregar filtros de tabla y ordenación a la vista de clientes (DSYS-04)</name>

  <read_first>
    - apps/dashboard/app/clientes/page.tsx (leer completo — ya migrado en Task 1)
    - apps/dashboard/lib/data.ts (leer para entender la estructura de Cliente y los tipos de estado)
    - .planning/phases/01-foundation/01-UI-SPEC.md líneas 498–539 (Table / Database View Contract — especificaciones exactas de chips y tabla)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-09: lib/data.ts NO se modifica — solo UI)
  </read_first>

  <files>
    apps/dashboard/app/clientes/page.tsx
  </files>

  <action>
La página de clientes actualmente es un Server Component (sin 'use client'). Para agregar filtros y ordenación con estado local de React, debe convertirse en Client Component.

**Paso 1 — Convertir a Client Component:**
Agregar `'use client'` como primera línea del archivo app/clientes/page.tsx.

Importante: Como Client Component, los imports de `lib/data.ts` siguen funcionando — son imports de módulos TS, no de APIs externas. El data.ts es código puro sin server-only APIs.

**Paso 2 — Agregar estado de filtros y ordenación:**
```typescript
'use client'
import { useState } from 'react'
import { ArrowUp, ArrowDown } from 'lucide-react'
// ... resto de imports existentes

type FilterEstado = 'todos' | 'estable' | 'atencion' | 'riesgo' | 'critico'
type SortColumn = 'nombre' | 'healthScore' | 'plan' | null
type SortDirection = 'asc' | 'desc'

export default function Clientes() {
  const [filtroEstado, setFiltroEstado] = useState<FilterEstado>('todos')
  const [sortCol, setSortCol] = useState<SortColumn>(null)
  const [sortDir, setSortDir] = useState<SortDirection>('asc')

  // Filtrado
  const clientesFiltrados = clientes.filter(c => {
    if (filtroEstado === 'todos') return true
    return c.estado === filtroEstado
  })

  // Ordenación
  const clientesOrdenados = [...clientesFiltrados].sort((a, b) => {
    if (!sortCol) return 0
    const mult = sortDir === 'asc' ? 1 : -1
    if (sortCol === 'nombre') return mult * a.nombre.localeCompare(b.nombre)
    if (sortCol === 'healthScore') return mult * (a.healthScore - b.healthScore)
    if (sortCol === 'plan') return mult * a.plan.localeCompare(b.plan)
    return 0
  })

  function handleSort(col: SortColumn) {
    if (sortCol === col) {
      if (sortDir === 'asc') setSortDir('desc')
      else { setSortCol(null); setSortDir('asc') }
    } else {
      setSortCol(col)
      setSortDir('asc')
    }
  }
  // ...
```

**Paso 3 — Chips de filtro** (encima de la tabla, per UI-SPEC):

```typescript
const filtros: { value: FilterEstado; label: string }[] = [
  { value: 'todos', label: 'Todos' },
  { value: 'estable', label: 'Estable' },
  { value: 'atencion', label: 'Atención' },
  { value: 'riesgo', label: 'En Riesgo' },
  { value: 'critico', label: 'Crítico' },
]
```

Renderizar chips:
```tsx
<div style={{ display: 'flex', gap: 8, flexWrap: 'wrap', padding: '12px 0' }}>
  {filtros.map(f => {
    const active = filtroEstado === f.value
    return (
      <button
        key={f.value}
        onClick={() => setFiltroEstado(f.value)}
        style={{
          fontSize: 12,
          fontWeight: 400,
          padding: '4px 8px',
          borderRadius: 20,
          border: `1px solid ${active ? 'rgba(35,131,226,0.3)' : 'var(--border)'}`,
          background: active ? 'var(--accent-bg)' : 'var(--surface-1)',
          color: active ? 'var(--accent)' : 'var(--text-2)',
          cursor: 'pointer',
          transition: 'background 80ms ease',
        }}
        className={!active ? 'hover:bg-[var(--surface-2)]' : ''}
      >
        {f.label}
      </button>
    )
  })}
  {filtroEstado !== 'todos' && (
    <button
      onClick={() => setFiltroEstado('todos')}
      style={{ fontSize: 12, color: 'var(--accent)', background: 'none', border: 'none', cursor: 'pointer', padding: '4px 8px' }}
    >
      Limpiar filtros
    </button>
  )}
</div>
```

**Paso 4 — Encabezados de tabla ordenables** (per UI-SPEC):

Para las columnas sortables (Nombre, Health Score, Plan), agregar onClick y el ícono de flecha:

```tsx
function SortIndicator({ col, sortCol, sortDir }: { col: SortColumn; sortCol: SortColumn; sortDir: SortDirection }) {
  if (sortCol !== col) return null
  return sortDir === 'asc' ? <ArrowUp className="inline w-3 h-3 ml-1" /> : <ArrowDown className="inline w-3 h-3 ml-1" />
}
```

Encabezado th sortable:
```tsx
<th
  style={{
    fontSize: 12, fontWeight: 400, color: 'var(--text-3)',
    textTransform: 'uppercase', letterSpacing: '0.05em',
    height: 36, padding: '0 16px', textAlign: 'left',
    cursor: 'pointer', userSelect: 'none',
    background: 'var(--surface-1)',
    borderBottom: '1px solid var(--border)',
  }}
  onClick={() => handleSort('nombre')}
  className="hover:text-[var(--text-2)]"
>
  Cliente <SortIndicator col="nombre" sortCol={sortCol} sortDir={sortDir} />
</th>
```

**Paso 5 — Usar clientesOrdenados en lugar de clientes** en el map de las filas de la tabla.

**IMPORTANTE:** Verificar qué campos tiene el tipo `Cliente` en `lib/data.ts` antes de escribir el filtrado — usar el campo de estado que realmente exista (puede ser `estado` u otro nombre). Adaptar el filtrado según la estructura real del tipo. NO modificar lib/data.ts (D-09).

Si el tipo `Estado` en data.ts usa diferentes valores de string (por ejemplo 'estable' | 'atencion' | 'riesgo' | 'critico'), el filtrado debe matchear esos valores exactos.
  </action>

  <verify>
    <automated>cd /Users/juancruzmagallanes/Documents/Claude/Programador:Desarrollador/apps/dashboard && npm run build 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - apps/dashboard/app/clientes/page.tsx contiene `'use client'` como primera línea
    - apps/dashboard/app/clientes/page.tsx contiene `useState` importado de 'react'
    - apps/dashboard/app/clientes/page.tsx contiene `filtroEstado` (estado de filtro activo)
    - apps/dashboard/app/clientes/page.tsx contiene `handleSort` (función de ordenación)
    - apps/dashboard/app/clientes/page.tsx contiene `ArrowUp` importado de 'lucide-react'
    - apps/dashboard/app/clientes/page.tsx contiene `Limpiar filtros` (UI del chip reset)
    - `npm run build` sale con código 0 sin errores TypeScript
  </acceptance_criteria>

  <done>Vista de clientes convertida a Client Component con chips de filtro por estado y ordenación por columna. DSYS-04 cumplido en la página más relevante del MVP.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Client Component state | filtroEstado y sortCol son estado local de React — no hay datos externos |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | app/clientes/page.tsx — Client Component | accept | Convierte Server a Client Component — los datos de clientes son del seed data estático que ya era accesible; no hay API keys ni secretos expuestos en este componente |
| T-03-02 | Tampering | filtros y sort — estado local | accept | Estado local de React (useState) — no persiste, no afecta otros usuarios, no hay superficie de ataque; la manipulación solo afecta la propia vista del usuario |
| T-03-03 | Information Disclosure | token migration — tokens oscuros eliminados | accept | Migración de tokens CSS — no hay superficie de seguridad; los valores visuales son públicos por naturaleza |
</threat_model>

<verification>
Después de completar ambas tareas:

1. Build compila: `cd apps/dashboard && npm run build` — debe salir con código 0
2. Tokens viejos eliminados (comando principal de verificación):
   ```bash
   grep -rn "var(--s1)\b\|var(--s2)\b\|var(--s3)\b\|var(--txt)\b\|var(--txt2)\b\|var(--txt3)\b\|var(--border2)\b" \
     apps/dashboard/app/ apps/dashboard/components/TareasCliente.tsx
   ```
   Debe retornar 0 líneas.
3. Clases helper eliminadas:
   ```bash
   grep -rn "hover-row\|hover-row2\|hover-border\b" \
     apps/dashboard/app/ apps/dashboard/components/TareasCliente.tsx
   ```
   Debe retornar 0 líneas.
4. Filtros en clientes: `grep -c "filtroEstado" apps/dashboard/app/clientes/page.tsx` — debe retornar >= 2
5. Sort en clientes: `grep -c "handleSort" apps/dashboard/app/clientes/page.tsx` — debe retornar >= 2
</verification>

<success_criteria>
- Los 9 archivos (app/page.tsx, app/clientes/page.tsx, app/clientes/[id]/page.tsx, app/alertas/page.tsx, app/tareas/page.tsx, app/herramientas/page.tsx, app/herramientas/n8n/page.tsx, y cualquier otra página existente + TareasCliente.tsx) no contienen ningún token CSS de la paleta oscura vieja
- grep de verificación retorna 0 para --s1, --s2, --s3, --txt, --txt2, --txt3, --border2, hover-row, hover-row2, hover-border en todos los archivos de app/ y components/
- app/clientes/page.tsx tiene chips de filtro por estado (Todos, Estable, Atención, En Riesgo, Crítico) y ordenación por columna (Nombre, Health Score, Plan)
- `npm run build` sale con código 0
</success_criteria>

<output>
Después de completar este plan, crear `.planning/phases/01-foundation/01-03-SUMMARY.md` con:
- Archivos migrados
- Resultado del grep de verificación (0 tokens viejos)
- Descripción de los filtros implementados
- Cualquier desviación del plan
</output>
