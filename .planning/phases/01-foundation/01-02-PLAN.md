---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - apps/dashboard/components/Sidebar.tsx
  - apps/dashboard/components/ui/Badge.tsx
  - apps/dashboard/components/ui/Card.tsx
  - apps/dashboard/components/ui/StatBlock.tsx
  - apps/dashboard/components/ui/AlertRow.tsx
  - apps/dashboard/components/ui/HealthBadge.tsx
  - apps/dashboard/components/ui/MetricRow.tsx
autonomous: true
requirements:
  - DSYS-02
  - DSYS-03

must_haves:
  truths:
    - "El sidebar se colapsa a 56px y se expande a 240px con transición de 200ms al hacer click en el toggle"
    - "El estado collapse/expand persiste en localStorage bajo la key 'sidebar_collapsed'"
    - "El sidebar muestra íconos Lucide React correctos para cada ítem de navegación"
    - "El ítem activo tiene background var(--accent-bg) y color var(--accent)"
    - "El footer del sidebar tiene links de Settings y botón Salir con signOut()"
    - "Los 6 componentes UI exportan named exports con TypeScript interfaces exactas del UI-SPEC"
    - "Badge renderiza con las 5 variantes y los colores semánticos correctos"
    - "StatBlock muestra value en 28px weight 600 con font-variant-numeric tabular-nums"
    - "HealthBadge mapea score 80-100 a verde, 60-79 a amber, 40-59 a naranja, 0-39 a rojo"
  artifacts:
    - path: "apps/dashboard/components/Sidebar.tsx"
      provides: "Sidebar colapsable con Lucide icons, localStorage, signOut"
      exports: ["default function Sidebar"]
    - path: "apps/dashboard/components/ui/Badge.tsx"
      provides: "Status badge con 5 variantes"
      exports: ["Badge"]
    - path: "apps/dashboard/components/ui/Card.tsx"
      provides: "Contenedor de card con padding opcional"
      exports: ["Card"]
    - path: "apps/dashboard/components/ui/StatBlock.tsx"
      provides: "KPI metric block con Display typography"
      exports: ["StatBlock"]
    - path: "apps/dashboard/components/ui/AlertRow.tsx"
      provides: "Fila de alerta con tipo semántico"
      exports: ["AlertRow"]
    - path: "apps/dashboard/components/ui/HealthBadge.tsx"
      provides: "Health score badge con color mapping"
      exports: ["HealthBadge"]
    - path: "apps/dashboard/components/ui/MetricRow.tsx"
      provides: "Par label/valor horizontal"
      exports: ["MetricRow"]
  key_links:
    - from: "apps/dashboard/components/Sidebar.tsx"
      to: "next-auth/react"
      via: "import { signOut } from 'next-auth/react'"
      pattern: "signOut"
    - from: "apps/dashboard/components/ui/AlertRow.tsx"
      to: "apps/dashboard/components/ui/Badge.tsx"
      via: "import { Badge } from '@/components/ui/Badge'"
      pattern: "Badge"
---

<objective>
Reescribir completamente Sidebar.tsx con Lucide React icons, collapse/expand con localStorage, nuevos tokens del design system y botón de logout. Crear los 6 componentes UI en components/ui/ con named exports, TypeScript interfaces exactas del UI-SPEC y especificaciones visuales completas.

Purpose: Entregar DSYS-02 (sidebar colapsable) y DSYS-03 (componentes base reutilizables). Estos componentes son los bloques fundamentales que todas las fases siguientes usan para construir UI.

Output: Sidebar.tsx completamente reescrito + 6 archivos en components/ui/ (Badge, Card, StatBlock, AlertRow, HealthBadge, MetricRow).
</objective>

<execution_context>
@/Users/juancruzmagallanes/Documents/Claude/get-shit-done/workflows/execute-plan.md
@/Users/juancruzmagallanes/Documents/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Contrato de tipos del UI-SPEC para los 6 componentes — copiar exactamente -->

```typescript
// Badge — components/ui/Badge.tsx
interface BadgeProps {
  variant: 'critical' | 'warning' | 'opportunity' | 'stable' | 'neutral'
  size?: 'sm' | 'md'
  children: React.ReactNode
}
// Visual: padding 4px 8px, font-size 12px, weight 400, border-radius 4px
// Colores por variante (del UI-SPEC Color section):
//   critical:    bg #FFF0F0, border #FFC5C5, text #C0392B
//   warning:     bg #FFFBEB, border #FDE68A, text #92400E
//   opportunity: bg #F0FDF4, border #A7F3D0, text #065F46
//   stable:      bg #F0FDF4, border #A7F3D0, text #065F46
//   neutral:     bg var(--surface-1), border var(--border), text var(--text-2)

// Card — components/ui/Card.tsx
interface CardProps {
  children: React.ReactNode
  className?: string
  noPadding?: boolean
}
// Visual: bg var(--bg), border 1px var(--border), border-radius 8px,
//         padding 20px (default), 0 (noPadding=true), box-shadow 0 1px 2px rgba(0,0,0,0.04)

// StatBlock — components/ui/StatBlock.tsx
interface StatBlockProps {
  label: string
  value: string | number
  sub?: string
  variant?: 'default' | 'critical'
}
// Visual: container bg var(--surface-1), border var(--border), radius 8px, padding 20px 24px
//         dot: 6px circle (--critical si variant=critical, --opportunity si default)
//         label: 12px, weight 400, text-3, uppercase, letter-spacing 0.06em
//         value: 28px, weight 600, text-1 (o --critical), line-height 1.2, margin-top 8px, tabular-nums
//         sub: 12px, weight 400, text-3, margin-top 8px

// AlertRow — components/ui/AlertRow.tsx
interface AlertRowProps {
  titulo: string
  clienteNombre: string
  fecha: string
  tipo: 'critico' | 'warning' | 'oportunidad'
  onClick?: () => void
}
// Visual: flex, gap 12px, padding 12px 20px, border-bottom 1px var(--border)
//         hover: bg var(--surface-1) si onClick
//         dot: 6px circle (--critical / --warning / --opportunity según tipo)
//         Badge component con variante mapeada desde tipo

// HealthBadge — components/ui/HealthBadge.tsx
interface HealthBadgeProps {
  score: number  // 0-100
  showLabel?: boolean
}
// Mapeo score → colores:
//   80-100: text #065F46, bg #F0FDF4, border #A7F3D0
//   60-79:  text #92400E, bg #FFFBEB, border #FDE68A
//   40-59:  text #9A3412, bg #FFF7ED, border #FED7AA
//   0-39:   text #C0392B, bg #FFF0F0, border #FFC5C5
// Visual: inline-flex, gap 4px, padding 4px 8px, border-radius 4px, border 1px
//         score: 14px, weight 600, tabular-nums
//         label (si showLabel): 12px, weight 400, mismo color

// MetricRow — components/ui/MetricRow.tsx
interface MetricRowProps {
  label: string
  value: string | number
  unit?: string
  dimLabel?: boolean
}
// Visual: flex justify-between align-center, padding 8px 0
//         border-bottom 1px var(--border) (excepto last-child)
//         label: 14px, weight 400, text-2
//         value: 14px, weight 600, text-1, tabular-nums
//         unit: 12px, weight 400, text-3, margin-left 4px
```

```typescript
// Sidebar — estructura de navegación del UI-SPEC
// Íconos Lucide a usar (className="w-4 h-4"):
//   LayoutDashboard - Dashboard
//   Users - Todos los clientes
//   Building2 - cada cliente individual (sub-item)
//   CheckSquare - Tareas
//   Bell - Alertas
//   Wrench - Todas las herramientas
//   Workflow - n8n (sub-item)
//   MessageSquare - Chatwoot (sub-item) [nota: usar MessageSquare, no Chat]
//   Cpu - OpenAI (sub-item)
//   Table - Airtable (sub-item)
//   Hash - Slack (sub-item)
//   Settings - Configuración (footer)
//   LogOut - Salir (footer)
//   PanelLeftClose - toggle cuando expandido
//   PanelLeftOpen - toggle cuando colapsado
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reescribir Sidebar.tsx con Lucide, collapse/expand y signOut</name>

  <read_first>
    - apps/dashboard/components/Sidebar.tsx (leer el archivo completo actual — se va a reemplazar)
    - .planning/phases/01-foundation/01-UI-SPEC.md líneas 272–345 (Sidebar Contract completo: dimensiones, nav items, estados, navigation structure)
    - .planning/phases/01-foundation/01-PATTERNS.md líneas 598–668 (patrón de reescritura, localStorage, active link, sub-componentes)
    - .planning/phases/01-foundation/01-RESEARCH.md líneas 546–548 (notas sobre SessionProvider — NO agregar en Phase 1)
  </read_first>

  <files>apps/dashboard/components/Sidebar.tsx</files>

  <action>
Reescribir components/Sidebar.tsx íntegramente. El archivo actual usa emoji icons y estilos oscuros — reemplazar todo. Seguir el contrato del UI-SPEC Sidebar Contract.

**Estructura del archivo:**

```typescript
'use client'
import { useState, useEffect } from 'react'
import Link from 'next/link'
import { usePathname } from 'next/navigation'
import { signOut } from 'next-auth/react'
import {
  LayoutDashboard, Users, Building2, CheckSquare, Bell,
  Wrench, Workflow, MessageSquare, Cpu, Table, Hash,
  Settings, LogOut, PanelLeftClose, PanelLeftOpen,
} from 'lucide-react'
import { clientes } from '@/lib/data'
```

**Sub-componentes internos (no exportados):**

`function SectionLabel({ label, collapsed }: { label: string; collapsed: boolean })`:
- Si collapsed: no renderizar nada (return null)
- Si expandido: `<div style={{ fontSize: 12, fontWeight: 600, color: 'var(--text-3)', textTransform: 'uppercase', letterSpacing: '0.08em', padding: '8px 16px 4px' }}>{label}</div>`

`function NavItem({ href, label, icon: Icon, indent = false, collapsed }: { href: string; label: string; icon: React.ElementType; indent?: boolean; collapsed: boolean })`:
- Usar `usePathname()` para detectar ruta activa
- Active logic: `const active = path === href || (href !== '/' && path.startsWith(href))`
- Height: 32px, border-radius: 6px, margin: 2px 4px
- Padding expandido: `0 12px` (con indent: `paddingLeft: 32px`)
- Padding colapsado: centrado, `0 16px`
- Estado default: bg transparent, text var(--text-2), icon var(--text-3)
- Estado hover: bg var(--surface-2), text var(--text-1), icon var(--text-2)
- Estado active: bg var(--accent-bg), text var(--accent), icon var(--accent)
- Cuando collapsed: mostrar solo ícono centrado con `title={label}` nativo (tooltip HTML)
- Cuando expandido: ícono + label
- aria-current="page" en el active item
- Transición: `transition: background 80ms ease`

**Componente principal Sidebar:**

```typescript
export default function Sidebar() {
  const [collapsed, setCollapsed] = useState(false)

  useEffect(() => {
    try {
      const saved = localStorage.getItem('sidebar_collapsed')
      if (saved) setCollapsed(JSON.parse(saved))
    } catch {}
  }, [])

  function toggle() {
    const next = !collapsed
    setCollapsed(next)
    try { localStorage.setItem('sidebar_collapsed', JSON.stringify(next)) } catch {}
  }

  // ... render
}
```

**Layout del sidebar (CSS):**
- `position: fixed, top: 0, left: 0, height: 100vh`
- Width: `collapsed ? 56 : 240` con `transition: 'width 200ms ease'`
- Background: `var(--surface-1)`, border-right: `1px solid var(--border)`
- z-index suficiente para estar sobre el contenido

**Workspace header (height 52px):**
- Logo mark: 26×26px, border-radius 6px, background var(--accent), letra "A" blanca, font-size 14px weight 600
- Workspace name "Agency" (14px, weight 600, text-1) — oculto cuando collapsed con `overflow: hidden, opacity: 0, width: 0`
- Toggle button: PanelLeftClose cuando expandido, PanelLeftOpen cuando colapsado
  - aria-label: "Colapsar sidebar" / "Expandir sidebar"
  - style: `background: none, border: none, cursor: pointer, color: var(--text-3), display: flex, alignItems: center`

**Nav structure** (per UI-SPEC Navigation Structure):
```
<SectionLabel label="Clientes" collapsed={collapsed} />
NavItem href="/clientes" label="Todos los clientes" icon={Users}
{clientes.map(c => <NavItem key={c.id} href={`/clientes/${c.id}`} label={c.nombre} icon={Building2} indent collapsed={collapsed} />)}

<SectionLabel label="Gestión" collapsed={collapsed} />
NavItem href="/tareas" label="Tareas" icon={CheckSquare}
NavItem href="/alertas" label="Alertas" icon={Bell}

<SectionLabel label="Herramientas" collapsed={collapsed} />
NavItem href="/herramientas" label="Todas" icon={Wrench}
NavItem href="/herramientas/n8n" label="n8n" icon={Workflow} indent
NavItem href="/herramientas/chatwoot" label="Chatwoot" icon={MessageSquare} indent
NavItem href="/herramientas/openai" label="OpenAI" icon={Cpu} indent
NavItem href="/herramientas/airtable" label="Airtable" icon={Table} indent
NavItem href="/herramientas/slack" label="Slack" icon={Hash} indent
```

Nota: Dashboard navitem va ANTES del primer SectionLabel, sin sección:
```
NavItem href="/" label="Dashboard" icon={LayoutDashboard}
```

**Footer (border-top: 1px var(--border), height 52px, padding 0 16px):**
- Link a /settings: ícono Settings + label "Configuración" (solo en expandido)
- Botón Salir: ícono LogOut + label "Salir" (solo en expandido)
  - onClick: `() => signOut({ callbackUrl: '/login' })`
  - title="Salir" (tooltip nativo en collapsed state)
  - aria-label="Salir"
  - style: button plain, cursor pointer, color var(--text-2)

La transición de labels cuando se expande: usar opacity 0→1 con `transition: 'opacity 100ms ease'` y delay de 80ms (via `transitionDelay: collapsed ? '0ms' : '80ms'`).
  </action>

  <verify>
    <automated>cd /Users/juancruzmagallanes/Documents/Claude/Programador:Desarrollador/apps/dashboard && npm run build 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - apps/dashboard/components/Sidebar.tsx contiene `'use client'` como primera línea
    - apps/dashboard/components/Sidebar.tsx contiene `from 'lucide-react'`
    - apps/dashboard/components/Sidebar.tsx contiene `signOut` importado de 'next-auth/react'
    - apps/dashboard/components/Sidebar.tsx contiene `sidebar_collapsed` (localStorage key)
    - apps/dashboard/components/Sidebar.tsx contiene `PanelLeftClose` y `PanelLeftOpen`
    - apps/dashboard/components/Sidebar.tsx contiene `export default function Sidebar(`
    - apps/dashboard/components/Sidebar.tsx contiene `aria-current="page"`
    - apps/dashboard/components/Sidebar.tsx NO contiene emojis como íconos (⊞, ◈, etc.)
    - `npm run build` sale con código 0
  </acceptance_criteria>

  <done>Sidebar.tsx completamente reescrito con Lucide React icons, collapse/expand con localStorage persistence, tokens nuevos, active state correcto y botón de logout funcional.</done>
</task>

<task type="auto">
  <name>Task 2: Crear los 6 componentes UI en components/ui/</name>

  <read_first>
    - .planning/phases/01-foundation/01-UI-SPEC.md líneas 349–495 (Component Contracts completos — especificaciones exactas de cada componente)
    - .planning/phases/01-foundation/01-PATTERNS.md líneas 308–533 (patrones de cada componente con código de ejemplo)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-11: named exports, props tipadas)
  </read_first>

  <files>
    apps/dashboard/components/ui/Badge.tsx,
    apps/dashboard/components/ui/Card.tsx,
    apps/dashboard/components/ui/StatBlock.tsx,
    apps/dashboard/components/ui/AlertRow.tsx,
    apps/dashboard/components/ui/HealthBadge.tsx,
    apps/dashboard/components/ui/MetricRow.tsx
  </files>

  <action>
Crear el directorio `apps/dashboard/components/ui/` y los 6 archivos. Todos son named exports (D-11). Ninguno necesita `'use client'` — son Server Components por defecto (no tienen hooks ni event handlers propios, excepto AlertRow que tiene onClick opcional).

**IMPORTANTE:** AlertRow recibe `onClick?: () => void` — si se pasa onClick, el componente necesita `'use client'`. Solución: hacer AlertRow un Server Component que renderiza un `<button>` o `<div>` con el handler. Si onClick está presente en el componente que lo usa, ese componente ya es Client Component. AlertRow en sí puede ser Server Component y recibir el prop.

**Alternativa más simple:** todos los componentes sin `'use client'` — los consumer components que los usan ya son Client o Server según su uso. Los handlers se pasan como props. Esto es correcto en React/Next.js.

**Badge.tsx:**
```typescript
interface BadgeProps {
  variant: 'critical' | 'warning' | 'opportunity' | 'stable' | 'neutral'
  size?: 'sm' | 'md'
  children: React.ReactNode
}

const variantStyles: Record<BadgeProps['variant'], { bg: string; border: string; color: string }> = {
  critical:    { bg: '#FFF0F0', border: '#FFC5C5', color: '#C0392B' },
  warning:     { bg: '#FFFBEB', border: '#FDE68A', color: '#92400E' },
  opportunity: { bg: '#F0FDF4', border: '#A7F3D0', color: '#065F46' },
  stable:      { bg: '#F0FDF4', border: '#A7F3D0', color: '#065F46' },
  neutral:     { bg: 'var(--surface-1)', border: 'var(--border)', color: 'var(--text-2)' },
}

export function Badge({ variant, children }: BadgeProps) {
  const s = variantStyles[variant]
  return (
    <span
      style={{
        display: 'inline-flex',
        alignItems: 'center',
        padding: '4px 8px',
        fontSize: 12,
        fontWeight: 400,
        lineHeight: 1.4,
        borderRadius: 4,
        border: `1px solid ${s.border}`,
        background: s.bg,
        color: s.color,
        whiteSpace: 'nowrap',
      }}
    >
      {children}
    </span>
  )
}
```

**Card.tsx:**
```typescript
interface CardProps {
  children: React.ReactNode
  className?: string
  noPadding?: boolean
}

export function Card({ children, className, noPadding = false }: CardProps) {
  return (
    <div
      className={className ?? ''}
      style={{
        background: 'var(--bg)',
        border: '1px solid var(--border)',
        borderRadius: 8,
        padding: noPadding ? 0 : 20,
        boxShadow: '0 1px 2px rgba(0,0,0,0.04)',
        overflow: noPadding ? 'hidden' : undefined,
      }}
    >
      {children}
    </div>
  )
}
```

**StatBlock.tsx:**
```typescript
interface StatBlockProps {
  label: string
  value: string | number
  sub?: string
  variant?: 'default' | 'critical'
}

export function StatBlock({ label, value, sub, variant = 'default' }: StatBlockProps) {
  const dotColor = variant === 'critical' ? 'var(--critical)' : 'var(--opportunity)'
  const valueColor = variant === 'critical' ? 'var(--critical)' : 'var(--text-1)'

  return (
    <div
      style={{
        background: 'var(--surface-1)',
        border: '1px solid var(--border)',
        borderRadius: 8,
        padding: '20px 24px',
      }}
    >
      <div style={{ display: 'flex', alignItems: 'center', gap: 6, marginBottom: 8 }}>
        <div style={{ width: 6, height: 6, borderRadius: '50%', background: dotColor, flexShrink: 0 }} />
        <span
          style={{
            fontSize: 12,
            fontWeight: 400,
            color: 'var(--text-3)',
            textTransform: 'uppercase',
            letterSpacing: '0.06em',
          }}
        >
          {label}
        </span>
      </div>
      <div
        style={{
          fontSize: 28,
          fontWeight: 600,
          color: valueColor,
          lineHeight: 1.2,
          marginTop: 8,
          fontVariantNumeric: 'tabular-nums',
        }}
      >
        {value}
      </div>
      {sub && (
        <div style={{ fontSize: 12, fontWeight: 400, color: 'var(--text-3)', marginTop: 8 }}>
          {sub}
        </div>
      )}
    </div>
  )
}
```

**AlertRow.tsx:**
```typescript
import { Badge } from '@/components/ui/Badge'
import type { BadgeProps } from '@/components/ui/Badge'  // si se exporta el type

// Si BadgeProps no se exporta, definir el mapping inline:
const tipoBadgeVariant: Record<AlertRowProps['tipo'], 'critical' | 'warning' | 'opportunity'> = {
  critico:    'critical',
  warning:    'warning',
  oportunidad: 'opportunity',
}

const tipoDotColor: Record<AlertRowProps['tipo'], string> = {
  critico:    'var(--critical)',
  warning:    'var(--warning)',
  oportunidad: 'var(--opportunity)',
}

const tipoLabel: Record<AlertRowProps['tipo'], string> = {
  critico:    'Crítico',
  warning:    'Warning',
  oportunidad: 'Oportunidad',
}

interface AlertRowProps {
  titulo: string
  clienteNombre: string
  fecha: string
  tipo: 'critico' | 'warning' | 'oportunidad'
  onClick?: () => void
}

export function AlertRow({ titulo, clienteNombre, fecha, tipo, onClick }: AlertRowProps) {
  return (
    <div
      onClick={onClick}
      style={{
        display: 'flex',
        alignItems: 'center',
        gap: 12,
        padding: '12px 20px',
        borderBottom: '1px solid var(--border)',
        cursor: onClick ? 'pointer' : 'default',
        transition: 'background 80ms ease',
      }}
      className={onClick ? 'hover:bg-[var(--surface-1)]' : ''}
    >
      <div
        style={{
          width: 6,
          height: 6,
          borderRadius: '50%',
          background: tipoDotColor[tipo],
          flexShrink: 0,
        }}
      />
      <div style={{ flex: 1, minWidth: 0 }}>
        <div
          style={{
            fontSize: 14,
            fontWeight: 400,
            color: 'var(--text-1)',
            overflow: 'hidden',
            textOverflow: 'ellipsis',
            whiteSpace: 'nowrap',
          }}
        >
          {titulo}
        </div>
        <div style={{ fontSize: 12, color: 'var(--text-3)', marginTop: 4 }}>
          {clienteNombre} · {fecha}
        </div>
      </div>
      <Badge variant={tipoBadgeVariant[tipo]}>{tipoLabel[tipo]}</Badge>
    </div>
  )
}
```

**HealthBadge.tsx:**
```typescript
interface HealthBadgeProps {
  score: number  // 0-100
  showLabel?: boolean
}

function getHealthColors(score: number) {
  if (score >= 80) return { text: '#065F46', bg: '#F0FDF4', border: '#A7F3D0', label: 'Estable' }
  if (score >= 60) return { text: '#92400E', bg: '#FFFBEB', border: '#FDE68A', label: 'Atención' }
  if (score >= 40) return { text: '#9A3412', bg: '#FFF7ED', border: '#FED7AA', label: 'En Riesgo' }
  return { text: '#C0392B', bg: '#FFF0F0', border: '#FFC5C5', label: 'Crítico' }
}

export function HealthBadge({ score, showLabel = false }: HealthBadgeProps) {
  const colors = getHealthColors(score)

  return (
    <span
      style={{
        display: 'inline-flex',
        alignItems: 'center',
        gap: 4,
        padding: '4px 8px',
        borderRadius: 4,
        border: `1px solid ${colors.border}`,
        background: colors.bg,
      }}
    >
      <span
        style={{
          fontSize: 14,
          fontWeight: 600,
          color: colors.text,
          fontVariantNumeric: 'tabular-nums',
        }}
      >
        {score}
      </span>
      {showLabel && (
        <span style={{ fontSize: 12, fontWeight: 400, color: colors.text }}>
          {colors.label}
        </span>
      )}
    </span>
  )
}
```

**MetricRow.tsx:**
```typescript
interface MetricRowProps {
  label: string
  value: string | number
  unit?: string
  dimLabel?: boolean
}

export function MetricRow({ label, value, unit, dimLabel = false }: MetricRowProps) {
  return (
    <div
      style={{
        display: 'flex',
        justifyContent: 'space-between',
        alignItems: 'center',
        padding: '8px 0',
        borderBottom: '1px solid var(--border)',
      }}
    >
      <span
        style={{
          fontSize: 14,
          fontWeight: 400,
          color: dimLabel ? 'var(--text-3)' : 'var(--text-2)',
        }}
      >
        {label}
      </span>
      <span style={{ display: 'flex', alignItems: 'baseline', gap: 2 }}>
        <span
          style={{
            fontSize: 14,
            fontWeight: 600,
            color: 'var(--text-1)',
            fontVariantNumeric: 'tabular-nums',
          }}
        >
          {value}
        </span>
        {unit && (
          <span style={{ fontSize: 12, fontWeight: 400, color: 'var(--text-3)', marginLeft: 4 }}>
            {unit}
          </span>
        )}
      </span>
    </div>
  )
}
```

NOTA sobre `last-child` en MetricRow: el border-bottom en el último elemento se puede eliminar con Tailwind `[&:last-child]:border-b-0` si se necesita en el futuro. En Phase 1, border-bottom en todos es aceptable.

**Verificar que el directorio components/ui/ existe** antes de crear los archivos.
  </action>

  <verify>
    <automated>cd /Users/juancruzmagallanes/Documents/Claude/Programador:Desarrollador/apps/dashboard && npm run build 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - Los 6 archivos existen en apps/dashboard/components/ui/
    - Cada archivo contiene `export function` (named export, no default) — D-11
    - Badge.tsx contiene las 5 variantes: 'critical' | 'warning' | 'opportunity' | 'stable' | 'neutral'
    - StatBlock.tsx contiene `fontVariantNumeric: 'tabular-nums'`
    - HealthBadge.tsx contiene la función getHealthColors con los 4 rangos (80, 60, 40, 0)
    - AlertRow.tsx contiene `import { Badge } from '@/components/ui/Badge'`
    - MetricRow.tsx contiene `fontVariantNumeric: 'tabular-nums'` en el value span
    - `npm run build` sale con código 0
    - NO hay archivos con `export default` en components/ui/
  </acceptance_criteria>

  <done>6 componentes UI creados en components/ui/ con named exports, TypeScript interfaces exactas del UI-SPEC y especificaciones visuales completas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sidebar → next-auth/react | signOut() es llamado desde componente cliente — limpia cookie de sesión |
| Browser → localStorage | Estado de collapse guardado en localStorage — dato no sensible |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing | Sidebar — signOut() | mitigate | signOut({ callbackUrl: '/login' }) limpia la cookie HttpOnly y redirige — no se puede falsificar el logout porque next-auth controla la cookie |
| T-02-02 | Information Disclosure | localStorage sidebar_collapsed | accept | Solo guarda un booleano (true/false) — no hay información sensible; riesgo inexistente |
| T-02-03 | Tampering | components/ui/ — props sin validación runtime | accept | Props validadas por TypeScript en build time; componentes son puramente presentacionales sin lógica de negocio — sin superficie de ataque |
| T-02-04 | Elevation of Privilege | Sidebar — nav items | accept | Nav items son links HTML estándar; el access control real lo hace proxy.ts antes del render — el sidebar no es una barrera de seguridad |
</threat_model>

<verification>
Después de completar ambas tareas:

1. Build compila: `cd apps/dashboard && npm run build` — debe salir con código 0
2. Sidebar usa Lucide: `grep -c "from 'lucide-react'" apps/dashboard/components/Sidebar.tsx` — debe retornar 1
3. localStorage key correcta: `grep -c "sidebar_collapsed" apps/dashboard/components/Sidebar.tsx` — debe retornar >= 2
4. 6 componentes existen: `ls apps/dashboard/components/ui/` — debe listar Badge.tsx Card.tsx StatBlock.tsx AlertRow.tsx HealthBadge.tsx MetricRow.tsx
5. Named exports: `grep -l "export default" apps/dashboard/components/ui/*.tsx` — debe retornar vacío (ningún default export en ui/)
6. AlertRow usa Badge: `grep -c "Badge" apps/dashboard/components/ui/AlertRow.tsx` — debe retornar >= 2
7. No emojis en Sidebar: `grep -c "⊞\|◈\|🏠\|📋" apps/dashboard/components/Sidebar.tsx` — debe retornar 0
</verification>

<success_criteria>
- Sidebar.tsx completamente reescrito: Lucide icons, collapse/expand 240px↔56px, localStorage key 'sidebar_collapsed', active state con accent color, footer con Settings link + Salir button (signOut)
- 6 componentes en components/ui/: Badge (5 variantes), Card (noPadding), StatBlock (Display 28px tabular-nums), AlertRow (usa Badge internamente), HealthBadge (4 rangos de score), MetricRow (label/valor/unidad)
- Todos los componentes UI: named exports (`export function X`), TypeScript interfaces completas, cero default exports
- `npm run build` sale con código 0 sin errores TypeScript
</success_criteria>

<output>
Después de completar este plan, crear `.planning/phases/01-foundation/01-02-SUMMARY.md` con:
- Archivos creados/modificados
- Decisiones de implementación tomadas
- Estado del build
- Cualquier desviación del plan
</output>
